/** @type {import("prettier").Config} */
const config = {
  singleQuote: true,
  semi: false,
  trailingComma: 'es5',
  tabWidth: 2,
  printWidth: 100,
  bracketSpacing: true,
  arrowParens: 'always',
  plugins: [],
}

module.exports = config
